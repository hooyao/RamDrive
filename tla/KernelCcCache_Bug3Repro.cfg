SPECIFICATION Spec

CONSTANTS
    Files = {f1, f2}
    MaxFileSize = 3
    \* Reproduce bug #3: FlushFileBuffers publishes default (zero) FspFileInfo.
    \* Run with this config to verify TLC catches the violation deterministically.
    BuggyCallbacks = {"FlushFileBuffers"}

INVARIANTS
    TypeOK
    CacheFileSizeMatchesActual
