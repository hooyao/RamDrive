SPECIFICATION Spec

CONSTANTS
    Files = {f1, f2}
    MaxFileSize = 3
    \* Empty BuggyCallbacks = the FIXED code path (FlushFileBuffers returns FileInfo).
    \* Invariant must hold.
    BuggyCallbacks = {}

INVARIANTS
    TypeOK
    CacheFileSizeMatchesActual
